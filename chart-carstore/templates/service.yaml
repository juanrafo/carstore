apiVersion: v1
kind: Service
metadata:
  name: {{ include "chart-carstore.fullname" . }}
  labels:
    {{- include "chart-carstore.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chart-carstore.selectorLabels" . | nindent 4 }}
