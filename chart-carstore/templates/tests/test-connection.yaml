apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chart-carstore.fullname" . }}-test-connection"
  labels:
    {{- include "chart-carstore.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "chart-carstore.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
